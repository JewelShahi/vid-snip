{
  "name": "vidsnip-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && CI=false npm run build"
  }
}